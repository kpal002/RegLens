# Prospective hypothesis — TEMPLATE (fill from the screen + a manual check)

> **This is a prospective, unverified hypothesis generated by RegLens** — not a validated
> result. It is included to show what the tool is *for*: screening noncoding variants for
> interpretable, uncharacterized regulatory mechanisms and proposing a **falsifiable**
> claim. Read it as a hypothesis with a named experiment that would kill it, not a finding.

Fill this in only after: (1) the variant surfaced from `run_discovery_screen` **in the
discovery quadrant**, (2) you **manually** confirmed no published mechanism, and (3) the
multi-agent produced an interpretation whose confidence and caveats you paste verbatim.

---

## Candidate: `<rsID>` — `<trait>` (`<gene>` locus)

**Why the screen surfaced it (deterministic, in-domain).** Screened in K562, where the
engine is validated (AUROC 0.716 on hematopoietic elements — making a claim here is inside
our calibrated competence, per the crossover result).

| signal | value |
|---|---|
| ChromBPNet \|Δ log-counts\| | `<e.g. 0.7x>` (engine fires) |
| Motif effect | `<TF>` `<created/disrupted>`, Δscore `<x>` — **concordant** with the Δ sign |
| GWAS trait | `<blood trait>` (GWAS Catalog, real association) |
| Literature (tool) | `<hit_count>` — *flag for manual check, not a novelty claim* |
| eQTL | `<present/absent>` |

## The hypothesis (RegLens multi-agent, confidence: **medium**)

> _Paste the agent's mechanism verbatim — the TF/element/gene/trait chain._

The confidence lands at **medium** by design: engine + concordant motif + GWAS all fire,
but there is **no corroborating eQTL/literature limb** — so the calibration machinery caps
it. The red-team's caveats, unedited:

> _Paste the red-team concerns verbatim (LD confounding, model-artifact risk, cell-type
> caveats). If the agent hedged, that hedge stays in — it is evidence the calibration is
> real, not cosmetic._

## Novelty (manual, not the literature tool)

**To our knowledge, no published mechanism** links this variant to `<gene>` via `<TF>`.
Checked: `<PubMed / Google Scholar / Open Targets queries you ran, and the date>`. We do
**not** claim "nobody knows" — only that a manual search found no characterization of *this
variant's* regulatory mechanism. If a prior report exists, this hypothesis is void.

## What would kill it (falsification)

A hypothesis is only scientific if it can be wrong. This one predicts:

1. **CRISPRi** of the `<element>` in erythroid cells (e.g. HUDEP-2 / primary erythroblasts)
   should **reduce `<gene>` expression** — if it doesn't, the enhancer→gene link fails.
2. **Allele-specific ATAC-seq** (or an MPRA) at this site should show **lower accessibility
   for the `<risk>` allele** — if the two alleles are equally accessible, the ChromBPNet
   prediction is wrong.
3. **`<TF>` ChIP / motif-KO** should show allele-dependent `<TF>` occupancy — absence
   falsifies the motif mechanism.

Any one negative result refutes the corresponding link.

## What we explicitly do **not** claim

- Not that this is causal (LD with the true causal variant is unresolved).
- Not that the effect size is clinically meaningful.
- Not that K562 accessibility equals the endogenous erythroid effect (modality gap).
- Not a discovery — a **hypothesis**, offered because the tool is calibrated enough that its
  hypotheses are worth testing.
